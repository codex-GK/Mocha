{
  "name": "automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "uiTest": "npx playwright test --grep @UI",
    "smoke": "npx playwright test --grep @Smoke",
    "functional": "npx playwright test --grep @Functional",
    "build": "echo 'Build script not needed for Playwright tests'",
    "test": "npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "allure-playwright": "^2.15.1"
  }
}
